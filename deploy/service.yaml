apiVersion: v1
kind: Service
metadata:
  name: kuis
  namespace: kuis
  labels:
    app.kubernetes.io/name: kuis
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kuis
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
